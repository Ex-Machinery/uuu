PNG_PLACEHOLDER: High-resolution PNG of the use case diagram (图1 用例图).

This file is a placeholder containing the mermaid source for the diagram.

Mermaid source:
```mermaid
%% 用例图示例（概览）
flowchart TB
  subgraph 用户
    U[用户]
    M[商家]
    C[配送员]
    A[管理员]
  end
  U -->|浏览/下单/支付| System[微信水果小程序]
  M -->|商品管理/接单| System
  C -->|配送状态更新| System
  A -->|统计/权限| System
```
